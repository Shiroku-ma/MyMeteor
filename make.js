function setup() {
    createCanvas(windowWidth, windowHeight);
    strokeWeight(0);
    fill(224,177,177)
}

var r = 0;

function draw() {
    
}

function mouseClicked(){
    circle(mouseX, mouseY, 3);
}